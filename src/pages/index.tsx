import { LinkButton } from "../components/Button";

const Home = () => {
    return(
        <section className="w-full  min-h-screen flex justify-center items-center">
            <LinkButton label="Enter site" path="/images" />
        </section>
    )
}

export default Home;
